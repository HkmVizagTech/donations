export const metadata = {
  title: "Admin Dashboard | ISKCON Charity Vizag",
  robots: {
    index: false,
    follow: false
  }
};

export default function AdminLayout({ children }) {
  return children;
}
